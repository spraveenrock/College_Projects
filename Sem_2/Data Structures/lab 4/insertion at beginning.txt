#include<stdio.h>
#include<stdlib.h>
struct node{
    int data;
    struct node* next;
    struct node* prev;
};
int main()
{
    int num,i,number,len,newnum;
    struct node* head;
    struct node* temp;
    struct node* newnode;
    struct node* Firstnode;
    head=(struct node*)malloc(sizeof(struct node));
    temp=malloc(sizeof(struct node));
    temp=head;
    printf("Enter the size in linked list: \n");
    scanf("%d",&num);
    temp->prev=NULL;
    newnode=malloc(sizeof(struct node));
    for(i=0;i<num;i++)
    {
        printf("element %d: ",i);
        scanf("%d",&number);
        temp->data=number;
        if(i!=num-1)
        { 
            temp->next=malloc(sizeof(struct node));
            newnode=temp;
            temp=temp->next;
            temp->prev=newnode;
        }
    }
    temp->next=NULL;
    printf("Enter the element to begin: ");
    scanf("%d",&newnum);
    temp=head;
    Firstnode=malloc(sizeof(struct node));
    Firstnode->data=newnum;
    Firstnode->prev=NULL;
    Firstnode->next=temp;
    temp->prev=Firstnode;
    while(head!=NULL)
    {
        printf("%d->",head->data);
        head=head->next;
    }
    printf("NULL\n");
    return 0;
}